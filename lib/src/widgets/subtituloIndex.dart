// ignore: file_names
import 'package:flutter/material.dart';

Padding subtitulo() {
    return const Padding(
            padding: EdgeInsets.all(20.0),
            child: Text(
              'Inicia sesión para acceder a la biblioteca más completa',
              textAlign: TextAlign.center,
              style: TextStyle(
                fontSize: 18,
                color: Colors.black,
              ),
            ),
          );
  }